{
  "name": "emails",
  "version": "0.0.0",
  "private": true,
  "description": "Scout transactional email templates (React Email). `pnpm build` renders them to static HTML/text artifacts in packages/api/templates/email/ — commit the outputs; the Python API only reads the artifacts.",
  "scripts": {
    "build": "tsx scripts/build.tsx",
    "dev": "email dev --dir emails"
  },
  "dependencies": {
    "@react-email/components": "^1.0.12",
    "@react-email/render": "^2.0.9",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^18.3.12",
    "react-email": "^6.6.5",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
